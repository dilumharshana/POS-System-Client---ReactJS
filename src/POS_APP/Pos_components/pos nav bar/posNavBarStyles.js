import { makeStyles } from "@material-ui/core";

export const styles = () =>
  makeStyles({
    root: {
      background: "#3f3d4d",
    },
    logoName: {
      fontFamily: "arial ,sans-serif",
      color: "#fff",
      fontWeight: "bold",
    },
    powerBtn: {
      marginLeft: "auto",
      border: "1px solid #fff",
      color: "#fff",
    },
  });
