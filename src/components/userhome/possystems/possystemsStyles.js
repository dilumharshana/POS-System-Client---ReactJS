import { makeStyles } from "@material-ui/core";

export const styles = () =>
  makeStyles({
    card: {
        width:'auto',
      width: "auto",
    },
    image: {
      width: "100%",
      height: 100,
    },
  });
