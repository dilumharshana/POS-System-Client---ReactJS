import { makeStyles } from "@material-ui/core";

export const styles = () =>
  makeStyles({
    paper: { borderRadius: "20px" },
    background: {
      background: "#8a4fff",
    },
    navlinks: {
      color: "#fff",
      fontSize: "15px",
    },
  });
